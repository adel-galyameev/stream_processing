1. Get the application URLs:

{{- if .Values.ingress.enabled }}
  http://{{ .Values.ingress.host }}/notification
  http://{{ .Values.ingress.host }}/order
  http://{{ .Values.ingress.host }}/billing
{{- end }}

2. Check deployment status:
  kubectl get pods -w
